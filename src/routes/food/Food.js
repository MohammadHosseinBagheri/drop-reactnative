import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Food = () => {
  return (
    <View style={{flex: 1}}>
      <Text>Food</Text>
    </View>
  );
};

export default Food;

const styles = StyleSheet.create({});
